# Orchestration Log — Summer (Backend Dev)
**Session:** 2026-03-20T0116 | **Agent:** claude-sonnet-4.5

## Assignment
Implement Azure Speech Mode backend — combined STT + chat completion + tool calling in a single HTTP endpoint.

## Work Completed

### 1. Async OpenAI Client
- Replaced sync `AzureOpenAI` with `AsyncAzureOpenAI` to avoid blocking the aiohttp event loop during chat completion calls

### 2. Synchronous SDK Wrapping
- Wrapped Speech SDK's `recognize_once()` (STT) in `run_in_executor()` to prevent event loop blocking
- Applied same pattern to TTS synthesis

### 3. Isolated SearchClient
- Created separate `SearchClient` instance for Azure Speech mode (vs sharing with Realtime pipeline)
- Prevents Speech mode load from starving WebSocket connections

### 4. Conversation History Management
- Implemented in-memory session storage with 20-message sliding window plus system message
- Enables multi-turn context without unbounded memory growth
- Acceptable for short-lived drive-thru sessions

### 5. Conditional Route Mounting
- Azure Speech endpoints mount only if `AZURE_SPEECH_KEY` and `AZURE_SPEECH_REGION` are present
- Realtime WebSocket mode unaffected if Speech env vars missing

### 6. Order Session Management
- Backend creates `order_state_singleton` session on first request
- Returns `session_id` in response for frontend to reference on subsequent calls

### 7. Tool Calling Loop
- Implemented full tool calling loop: `search` → `update_order` → `get_order`
- Integrates with existing tools.py logic

## Files Modified
- `app/backend/azurespeech.py` — new Azure Speech endpoint implementation
- `app/backend/app.py` — mounted conditional routes

## Verification
✅ All 100 tests pass

## Notes
- Conversation history is in-memory; lost on server restart (acceptable for drive-thru use case)
- No extras validation in Azure Speech path (could be added if needed)
- Frontend hook update required to consume `tool_results` from response (Morty's responsibility)
