# Grimace — History

## Sessions

_No sessions yet._

## Learnings

- **Menu Period Tagging (2026-03-23):** Added `menuPeriod` field to all 61 items in `app/frontend/src/data/menuItems.json`. Used Python script to parse JSON, insert field right after `name` key in each item object, and write back with UTF-8 encoding preserved. Category mapping: Breakfast→"breakfast", Burgers & Sandwiches→"lunch", Chicken & McNuggets®→"lunch", Fries/Sides/Drinks→"allDay", Sweets & Treats→"allDay". This enables Birdie's Breakfast/Lunch toggle on the frontend. ✅ Merged to decisions.md.
- **Chicken & McNuggets Menu Expansion (2026-03-23):** Expanded "Chicken & McNuggets®" category from 6→11 individual items (plus 10 unchanged Extra Value Meals). Renamed multi-size "Chicken McNuggets®" to "10 Piece Chicken McNuggets®" with single Standard size ($4.89). Added 4pc ($3.79), 6pc ($5.29), 20pc ($9.99), 40pc ($14.99) as separate items, plus standalone McCrispy® ($5.99). All new items follow existing JSON structure with `menuPeriod: "lunch"`, no `mealNumber`, empty image strings, and realistic allergens. Scanned Burgers & Sandwiches (10 items) — Big Mac, QPC variants, McDouble, Cheeseburger, Double Cheeseburger, Hamburger, McCrispy, Filet-O-Fish all present; no obvious McDonald's staples missing. Total menu: 66 items across 5 categories. Used Python script for safe JSON manipulation with OrderedDict to preserve key ordering.
- **Breakfast Extra Value Meals Expansion (2026-07-16):** Expanded Breakfast category from 5→10 Extra Value Meals (mealNumbers 1-10) to match lunch menu parity. Added: #6 Big Breakfast® with Hotcakes Meal ($7.99/1340cal), #7 Big Breakfast® Meal ($6.49/760cal), #8 Hotcakes Meal ($5.49/590cal), #9 Bacon, Egg & Cheese McGriddles® Meal ($6.99/780cal), #10 Sausage Biscuit with Egg Meal ($5.99/720cal). All items added directly to `menuItems.json` with proper structure (menuPeriod: "breakfast", mealNumber, Standard size, empty image, realistic allergens). Also updated `BREAKFAST_MEAL_NUMBER_MAP` in `tools.py` to map keys "6"-"10" so the voice AI resolves spoken meal numbers correctly. 126 existing tests still pass.
- **Prompt Externalization Architecture (2026-07-17):** Ported prompt/config externalization from Sonic project. Created `prompt_loader.py` (brand-parameterized YAML prompt loader with DEV_MODE hot-reload), `config_loader.py` (centralized YAML config with fail-fast validation), `config.yaml` (all magic numbers collected), `menu_utils.py` (McDonald's-specific size maps and category inference). Wired all four backend files: `app.py` (imports config+prompt_loader, removes 147-line hardcoded system_message), `rtmt.py` (config-driven echo cooldown/WS heartbeat, prompt_loader greeting), `tools.py` (config-driven cache/search/quantity limits, prompt_loader for errors/upsells/deltas), `order_state.py` (timezone-aware happy hour via ZoneInfo, config-driven tax/discount). All modules preserve hardcoded fallbacks for backward compatibility.
- **Phase 1 Processor Abstraction (2026-07-18):** Created the abstraction layer for cloud↔local processor switching. New files: `processor_base.py` (AbstractProcessor ABC — interface contract for handle_websocket, start/stop_background_tasks), `local_processor.py` (LocalPhi4Processor stub with import guards for onnxruntime_genai/piper, VAD buffer placeholder, WebSocket protocol stubs matching RTMiddleTier's message types), `processor_router.py` (ProcessorRouter — routes WS connections to cloud or local based on config.yaml `local_mode.enabled` or `?mode=` query param). Updated `app.py` to wire router (replaces direct rtmt.attach_to_app), added `/api/local-mode/status` endpoint. Added `local_mode` section to `config.yaml`. RTMiddleTier left untouched as a black box — router delegates to its `_websocket_handler` directly. All 159 app/rtmt/security tests pass unchanged.
- **Phase 3 Code Organization (2026-07-17):** Split rtmt.py (778 lines) into 3 modules: `session_manager.py` (SessionManager + ContextMonitor — session lifecycle, greeting state, concurrency limits, idle timeout, context window monitoring), `audio_pipeline.py` (EchoSuppressor class + verbose logging infrastructure + audio markers/constants + passthrough type sets), and a slimmed `rtmt.py` (RTMiddleTier, Tool, ToolResult, RTToolCall — imports from the new modules). Added `context` and `security` sections to config.yaml. Upgraded app.py with startup validation (required env vars → sys.exit(1)), enhanced /health endpoint (version + checks status), service connectivity probing, on_startup/on_shutdown hooks for background tasks (token refresh, idle checker). Updated test_app.py and test_performance.py to accommodate new startup flow (mock _check_service_connectivity, SystemExit instead of RuntimeError). All 202 tests pass.
- **Phase 5 Config Integration & Startup Wiring (2026-07-18):** Integrated local mode config into startup flow. Fixed duplicate `local_mode` section in config.yaml. Added `get_local_mode_config()` to config_loader.py — merges defaults ← YAML ← env var overrides (LOCAL_MODE_ENABLED, LOCAL_MODE_MODEL_PATH, LOCAL_MODE_DEVICE). Enhanced app.py: comprehensive `/api/local-mode/status` endpoint (8 fields: available, enabled, device, model_loaded, model_path, model_exists, tts_model, tts_available), `/health` now includes `local_mode` section, startup logs model dir size + TTS readiness + GPU detection, graceful degradation for missing models/deps/GPU (warnings only, never crashes). All 423 tests pass unchanged — cloud mode unaffected.
- **Local Menu Search & Offline Tools (2026-07-18):** Created `local_search.py` — in-memory keyword search over `menuItems.json` replacing Azure AI Search for offline mode. Loads 71 menu items at module init, implements tiered scoring (exact name=100, name-contains=80, category=40, description=20, token overlap tiebreaker). Reuses tools.py functions directly: `_expand_meal_number_query()`, `_format_size_human_readable()`, `MOCK_MACHINE_STATUS` OOS flagging, `_ICE_CREAM_MACHINE_KEYWORDS`. Output format is byte-for-byte compatible with cloud search (same `[{id}]: Item: ...` template with `\n-----\n` separator). Includes `_SearchCache` with identical 60s TTL / 128-entry config. `attach_local_tools()` registers search + order tools on LocalPhi4Processor with same schemas, decoupling local processor from `rtmt.tools` entirely. Updated `app.py` to call `attach_local_tools(local_processor, prompt_loader)` instead of `local_processor.tools = rtmt.tools`. All 559 tests pass (1 pre-existing upsell encoding failure unchanged). Cloud mode untouched.

## Team Updates (2026-03-23T12:18Z)

### From Scribe Orchestration
- ✅ Orchestration logs written for Grimace and Ronald background tasks
- ✅ Session log created for menu expansion & README work
- ✅ Both agents' outcomes: SUCCESS
- **Pending:** Decision merge, git commit, history summarization check

## Team Updates (2026-04-02T16:30Z)

### Offline Mode Phase Completion
- ✅ **Phase 1 (Grimace):** Processor abstraction layer complete — cloud/local routing in place
- ✅ **Phase 5 (Grimace):** Config integration finished — env var overrides, startup logging, health endpoint includes local mode telemetry
- **Decisions Merged:** #32–#41 captured (processor abstraction, config chain, startup wiring)
- **Tests:** All 423 passing, zero regressions in cloud mode
- **Next:** Orchestration logs written, session log created, inbox merged to decisions.md

## Learnings (continued)

- **Offline Silent Hang Root Cause (2026-07-19):** When Brian went offline, the /realtime WebSocket silently hung because: (1) config.yaml has `local_mode.enabled: false`, so ProcessorRouter defaults to "cloud"; (2) Frontend sends local mode preference via `extension.set_local_mode` WebSocket message AFTER connection, but routing decision happens AT connection time; (3) RTMiddleTier._forward_messages() tries ws_connect to Azure OpenAI at line 448 of rtmt.py — hangs on DNS resolution when offline, no error sent to client. Fix: added auto-fallback in ProcessorRouter — when mode resolves to "cloud" but Azure endpoint is unreachable and local processor exists, auto-routes to local. Added /api/local-mode/toggle POST endpoint for runtime mode switching. Made app.py startup non-fatal on missing Azure env vars when local mode is available.
- **Diagnostic Logging Pattern (2026-07-19):** Added `local-pipeline` logger across all local mode modules (processor_router.py, local_processor.py, phi4_model.py, piper_tts.py, whisper_stt.py, local_search.py). Every pipeline step is logged: WS connection → mode resolution → auto-fallback → model loading → audio receipt → VAD detection → Phi-4 inference (with timing) → Whisper STT → tool execution → Piper TTS (with chunk count) → response completion. Filter with `local-pipeline` logger name to see the full flow. Existing module-level loggers preserved for backward compatibility.
- **Diagnostics Endpoint (2026-07-19):** Added GET /api/diagnostics returning: current_mode, config_default_mode, runtime_mode_override, local_model_status, gpu_provider, tts_engine_status, stt_engine_status, last_error, websocket_total_connections, websocket_active_connections, cloud_processor status. Gives Brian a quick way to check system state without log diving.
- **Runtime Mode Toggle (2026-07-19):** Added POST /api/local-mode/toggle accepting `{"mode": "local"|"cloud"|"auto"}`. Updates ProcessorRouter._runtime_mode at runtime. "auto" clears the override and falls back to config default. Frontend can call this when user toggles local mode in the UI, solving the disconnect between UI preference and backend routing.
- **Graceful Offline Startup (2026-07-19):** app.py create_app() now checks local mode availability BEFORE requiring Azure env vars. If local processor is available but Azure vars are missing, logs a warning instead of sys.exit(1). Cloud RTMiddleTier creation is skipped entirely — ProcessorRouter handles None cloud_processor gracefully. This enables fully offline startup without any Azure credentials.

## Team Updates (2026-04-02T18:31Z)

### Offline Mode Diagnostics Iteration Complete
- ✅ **Grimace (Backend):** Auto-fallback to local mode when Azure unreachable (3s connectivity check), runtime `/api/local-mode/toggle` endpoint for frontend mode switching, comprehensive `/api/diagnostics` endpoint with 9 telemetry fields, `local-pipeline` logger across all 6 local processor modules (processor_router, local_processor, phi4_model, piper_tts, whisper_stt, local_search), graceful offline startup with non-fatal missing Azure env vars. Commit 03703dc.
- ✅ **Birdie (Frontend):** Fixed WebSocket URL to connect directly to `ws://localhost:8000/realtime` in local mode, readyState guards before session start, user-visible error feedback (`⚠️`), comprehensive console logging with `[WS]`, `[MIC]`, `[LOCAL-MODE]` prefixes. Commit c5d4518.
- **Decisions merged:** Both offline diagnostics decisions now in decisions.md
- **Session & Orchestration logs:** Written with full root cause analysis, fixes, and next actions
- **Test status:** 632 backend tests passing (baseline maintained), zero regressions. Frontend TypeScript clean, all 13 tests pass.
- **Next:** Birdie wires frontend toggle to `/api/local-mode/toggle`. Team monitors offline scenario with `local-pipeline` logger. Verify console logs show full diagnostic trace.

## Learnings (continued 2)

- **WebSocket Offline Fix — Three-Bug Root Cause (2026-07-20):** The /realtime WebSocket still failed offline despite auto-fallback because of three compounding bugs: (1) `_check_cloud_reachable()` had a 3s timeout and NO caching — every single WS connection repeated the slow network probe; (2) `RTMiddleTier._websocket_handler` called `_forward_messages()` with zero error handling — when `session.ws_connect()` to Azure failed (10-30s timeout), the exception propagated unhandled and the client WS closed with no error message; (3) Frontend never passes `?mode=local` in the WS URL, so the router always defaulted to "cloud" mode and hit the auto-fallback path. Fix: added cached cloud reachability (30s TTL, 1s/500ms timeout vs 3s/2s), startup probe so first WS is instant, explicit "local" fast-path in router that bypasses all cloud checks, and try/except in RTMiddleTier._websocket_handler around `_forward_messages()` that sends a structured error JSON to the client before closing. Diagnostics endpoint now shows `cloud_reachable` and `cloud_reachable_cache_age_s`. All 630+ tests pass unchanged.
- **Meal Component Breakdown (2026-07-21):** Ported Sonic's combo component logic to McDonald's, adapted for "Meal" terminology. Root cause of Brian's bug: order_state.py only checked for "combo" keyword but McDonald's menu uses "Meal" — so combo pivot/absorption never triggered for actual menu items. Fix: (1) Added `components: list[str] = []` to OrderItem model for sub-item display. (2) Created `_is_meal_or_combo()`, `_is_meal()`, `_extract_meal_entree()`, `_get_default_side()`, `_is_breakfast_meal()` helpers. (3) When a "Meal" is added, auto-populate components with entree + fries (or Hash Browns for breakfast) and auto-increment absorbed_sides. (4) Post-meal absorption: drinks added after a meal get absorbed into components without separate line item or extra charge. (5) Combo conversion: standalone entree auto-removed when meal version ordered. (6) Combo pivot: existing standalone drinks absorbed into newly added meal. (7) Updated `get_combo_requirements()` to use `_is_meal_or_combo()`. (8) Voice readback now mentions drink components. (9) tools.py delta text reflects absorption ("Diet Coke added to your Large Big Mac Meal"). (10) System prompt updated with SAME-UTTERANCE MEAL RULE from Sonic. 14 new tests, 680 total passing, zero regressions.
- **Combo = Meal Synonym Support (2026-07-21):** Brian noticed customers say "combo" interchangeably with "meal" at the drive-thru. The `_is_meal()` function only matched "meal", so "Big Mac Combo" wouldn't get auto-populated with entree + fries components. Fix: (1) Updated `_is_meal()` to match both "meal" and "combo" — now `_is_meal()` is functionally equivalent to `_is_meal_or_combo()` for McDonald's (both terms trigger fries auto-population). (2) Added COMBO_MEAL_SYNONYMS section to system prompt (priority 9) instructing the AI to treat "combo" and "meal" identically, always translate to official "Meal" name in tool calls. (3) Updated `_extract_meal_entree()` — already stripped " Combo" suffix, no change needed. (4) Updated 7 existing tests that expected old "combo without fries" behavior. (5) Added 7 new combo synonym tests covering: auto-population, drink-only requirement, drink absorption, meal conversion, breakfast hash browns, full order flow, and pivot absorption. (6) Fixed 1 tool_calling test (`test_incomplete_combo_triggers_system_hint`) that expected "side" in the hint — now only "drink" since fries are auto-included. 688 tests passing, 3 pre-existing failures (GPU detection + flaky upsell), zero regressions.
- **Meal Size Upgrade Fix — Two-Bug Fix (2026-07-22):** Brian reported two linked bugs: (1) AI didn't ask about size when converting entree→meal, silently defaulting to Medium; (2) Upgrading a meal's size from Medium→Large ejected the drink as a separate charged item because the only available tool actions were add/remove — forcing a remove+add cycle that lost the drink component. Root cause: `update_order` tool had no in-place modification capability, and system prompt didn't instruct size question during meal upsell. Fix: (1) Added `"modify"` action to `handle_order_update()` in `order_state.py` — finds existing item by name, updates size/price/display in place, and runs `_update_component_size()` on all meal components to sync size prefixes (Medium Fries→Large Fries, Medium Diet Coke→Large Diet Coke). Hash Browns and entrees are left unchanged. (2) Added `_update_component_size()` helper and `_SIZE_PREFIXES` constant. (3) Updated `update_order` tool schema in `tools.py` to include `"modify"` in the action enum with description guidance. (4) Added size-change delta text in `tools.py` ("Upgraded McCrispy Meal from Medium to Large"). (5) Updated system prompt: ORDERING section now instructs "ALWAYS ask what size (medium or large)" during meal conversion; SUGGESTIVE_SELLING updated to include size options in combo conversion script; new MEAL_SIZE_CHANGES section (priority 12.5) gives explicit instructions to use `modify` action instead of remove+add. (6) Added 6 new tests covering: full component preservation, no separate drink charge, fries+drink size prefix updates, downgrade scenario, breakfast Hash Browns unchanged, and combo synonym support. 694 tests passing (same 3 pre-existing failures), zero regressions.
- **Local Mode Session Tokens, Verbose Logging & File Logging (2026-07-22):** Three features that worked in cloud mode were missing from local/offline mode. (1) **Session Tokens:** LocalPhi4Processor now generates a per-connection session_token UUID and round_trip_index, emits `extension.session_metadata` on connection start and `extension.round_trip_token` after each `response.done` — same format as cloud mode (sessionToken, roundTripIndex, roundTripToken). Frontend SessionTokenPanel renders identically. Token counts from Phi-4 inference included in `response.done.usage`. Round state tracked via mutable `session_state` dict passed through the processing pipeline. (2) **Verbose Logging:** Handles `extension.set_verbose_logging` WebSocket message — sets per-connection `verbose` flag, activates `mcdonalds-verbose` logger. Added `_vlog()` calls at every pipeline step: audio received, resampling, Whisper STT start, Phi-4 inference start/end (with timing + token count), tool execution, TTS start/end (with chunk count), round trip advancement. (3) **Log to File:** Handles `extension.set_log_to_file` WebSocket message — reuses `create_verbose_file_handler()` / `remove_verbose_file_handler()` from `audio_pipeline.py`, writes to `app/backend/logs/verbose-*.log`. File handler cleaned up on WS disconnect. All three work fully offline — no network dependency. Parameters have defaults to preserve backward compatibility with existing tests. 694 tests pass (same 3 pre-existing failures), zero regressions.
- **Local Mode WebSocket Never Connects — Three-Layer Fix (2026-07-22):** Brian's recurring bug: toggling local mode ON and clicking mic always showed "Cannot connect to local server." Root cause was three compounding failure modes that prevented the WebSocket from ever staying in OPEN state. (1) **RTMiddleTier token warmup crash** — `self._token_provider()` in `__init__` (line 162 of rtmt.py) makes a synchronous Azure AD call. When offline with DefaultAzureCredential (no API key set), this THROWS, crashing RTMiddleTier's constructor. Since there was no try/except, the exception propagated through create_app() → ProcessorRouter was never created → /realtime route was never registered → all WebSocket connections got HTTP 404. Fix: wrapped in try/except with warning log. (2) **RTMiddleTier creation not guarded in app.py** — the entire cloud processor setup block (credential creation, RTMiddleTier init, attach_tools_rtmt) had no try/except. Any failure crashed create_app(). Fix: wrapped entire block in try/except; on failure, rtmt=None and local-only mode proceeds. conn_cfg, model_cfg, and app_secret are now initialized BEFORE the try block so they're always available. (3) **Local processor model load failure caused rapid WS cycle** — when handle_websocket failed to load models, it returned immediately. This closed the WebSocket, triggered auto-reconnect (react-use-websocket's shouldReconnect: () => true), which tried again, failed again, etc. The OPEN window was too brief for the user to ever click the mic. Fix: set `model_load_failed` flag, DON'T return — keep the WS alive in degraded mode. The message loop continues; audio processing responds with text error instead of trying inference. Also added: `/api/ws-test` echo WebSocket endpoint for isolation testing, startup confirmation logs ("✅ /realtime WebSocket route registered", processor availability). 694 tests pass (same 2 pre-existing failures), zero regressions.
- **Local Mode No Greeting & Silent Audio — Two-Bug Fix (2026-04-03):** Brian reported two linked bugs in local mode: WS connected ("🔌 WS: Connected ✅") but (1) no AI greeting played on session start, and (2) no transcription appeared when speaking. The verbose log showed ONLY settings toggles — zero audio processing. Root cause was a frontend↔backend interaction gap: (1) **No greeting:** In cloud mode, `session.update` triggers Azure OpenAI to generate a greeting autonomously (response.created → transcript deltas → audio deltas → response.done). Local mode's `session.update` handler just echoed `session.updated` — never generated any response. The frontend gates mic recording start on receiving `response.done` for the greeting (with a 3.5s safety timeout). Without a greeting, the mic was delayed 3.5s and no audio confirmation reached the user. Fix: after sending `session.updated`, local processor now calls `_send_text_response()` with "Welcome to McDonald's! What can I get started for you today?" — generates the full response sequence (response.created → transcript delta → TTS audio deltas → response.done) that the frontend needs to start the mic. (2) **Silent verbose log:** Local processor had `_vlog()` calls for inference and TTS but NONE for the message loop — no logging for `session.update` receipt, `input_audio_buffer.append` frames, VAD state changes, `speech_started`, or `commit`. Cloud mode logged audio frames every 50th frame. Fix: added `_vlog()` calls matching cloud mode's format for: session.update receipt, audio frame count (every 50 frames), VAD speech detection (with energy values), VAD silence→processing trigger (with duration/byte count), speech_started barge-in, and commit events. The verbose log file now captures the full local mode pipeline flow. 665 tests pass (same 2 pre-existing failures: GPU detection, flaky upsell), zero regressions.
- **Silent Model Load Failure — LOUD Diagnostics Fix (2026-04-03):** Brian's local mode had working WebSocket + VAD + audio frames but ZERO responses. Verbose log showed `_process_utterance_safe` never produced output, with no error messages. Root cause: `model_load_failed` was `True` (Phi-4 couldn't load), but the `model_load_failed` branch at line 298-301 sent a generic "models not loaded" message via WebSocket with NO verbose log entry — completely invisible in the verbose log. Fix: (1) Added `_vlog()` calls to both `model_load_failed` branches (VAD silence and commit) so dropped utterances show clearly in verbose log with the reason. (2) Added `_vlog()` + `print()` to `_process_utterance_safe` error handler so any crash during processing is visible in both verbose log AND terminal. (3) Added per-component error tracking: `_phi4_load_error`, `_tts_load_error`, `_stt_load_error` instance vars, `_format_load_error_detail()` helper. (4) Made model loading LOUD: every `_load_phi4`, `_load_tts`, `_load_stt` now prints to terminal AND pipeline_logger on both success and failure, with specific error messages naming the failing component and the fix command (e.g., "pip install onnxruntime-genai"). (5) Initial WS error message now shows detailed per-component breakdown as a transcript in the Guest Conversation panel. (6) Updated `/api/diagnostics` endpoint with `model_load_errors` object showing exact error strings for each component (overall, phi4, piper_tts, whisper_stt). Files changed: `local_processor.py` (error tracking, verbose logging, loud prints), `app.py` (diagnostics model_load_errors).
