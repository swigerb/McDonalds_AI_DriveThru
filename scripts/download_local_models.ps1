# Convenience wrapper for Windows — downloads local AI models
# Usage:
#   .\scripts\download_local_models.ps1
#   .\scripts\download_local_models.ps1 -cpu-only
#   .\scripts\download_local_models.ps1 -model-dir .\my_models

python scripts/download_local_models.py @args
