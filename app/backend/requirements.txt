aiohttp==3.14.3
azure-cognitiveservices-speech==1.38.0
azure-identity==1.25.3
azure-search-documents==12.0.0
azure-storage-blob==12.30.0
cffi==2.1.1
cryptography>=50.0.0,<51
gunicorn==23.0.0
openai==1.109.1
pydantic>=2,<3
python-dotenv==1.2.2
rich==15.0.0
PyYAML>=6.0
Jinja2>=3.1
tzdata>=2024.1; sys_platform == "win32"

# Local AI mode (offline inference)
# Install ONE of the following (GPU recommended):
#   pip install onnxruntime-genai              (CPU only — fallback)
#   pip install onnxruntime-genai-cuda         (NVIDIA CUDA — recommended for Linux)
#   pip install onnxruntime-genai-directml     (Windows GPU — recommended for Windows)
# Do NOT install both CPU and GPU variants — they conflict.
piper-tts>=1.2.0
soundfile>=0.12.1
huggingface-hub>=0.20.0

# Local STT (offline customer transcription)
faster-whisper>=1.0.0
# CUDA support requires nvidia-cublas-cu12 and nvidia-cudnn-cu12 (auto-installed by faster-whisper on CUDA systems)